---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pytime-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pytime-service
            port:
              number: 80
...
